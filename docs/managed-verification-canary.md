# Managed Verification Canary

Temporary same-repository probe for the managed verification executor and gate.
This update moves the pull-request head while a managed run is active.
The replacement head must require its own managed run.
