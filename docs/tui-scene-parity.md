# Browser and TUI scene parity

> Every visible browser kitchen-scene treatment, mapped to the shipped TUI
> pane (`server/src/tui/scene/` and the surrounding pieces in
> `server/src/tui/`). Status is exactly **Done** or **Deferred**; the
> notes column carries representation differences. The browser
> implementation (`client/src/scene/`, `client/src/theme/`,
> `client/src/chrome/`) was audited directly against the integration on
> `33da6d32`. The audit was performed as part of DOCMEDIA-01, corrected
> against shipped source as part of DOCMEDIA-02, and refreshed after the
> responsive tiled-scene redesign.
> A deferral that exists because it would only add a redundant signal at this
> resolution is still called out below; no gap is undocumented.

## Truth treatments and state machine

| Browser treatment                                                                            | Browser source                                                     | TUI treatment                                                                                                                                                                                                              | Status   | Notes                                                                                                                                |
| -------------------------------------------------------------------------------------------- | ------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| Five states (idle, working, blocked, done, ended)                                            | `client/src/state/store.ts` `AgentMachine.targetState`             | TUI scene `draw_agent` switches on `AgentState`; ended drops out of the active roster and lands on the bounded 86 board — verified by `ended_moves_to_board_and_truthful_status_survives` (`server/src/tui/scene/mod.rs`). | Done     | One-to-one mapping. Ended has no active cook sprite and appears only on the board.                                                   |
| State identity labels (`PREP`/`FIRE`/`AT THE PASS`/`PLATED`/`86'D`)                          | `client/src/scene/geometry.ts` `stationIdentityLabels` lines 36–63 | TUI station footer uses `{name} · PREP`, `{name} · FIRE`, `{name} · AT THE PASS`, or `{name} · PLATED ✓`; blocked stations also carry a `‼ BLOCKED {MM:SS} ‼` banner (`server/src/tui/scene/mod.rs`).                      | Done     | Verbatim label vocabulary plus elapsed blocked timing.                                                                               |
| `answered` ("ANSWER RECEIVED") suffix while `answerReceivedUntil > now` and state is working | `geometry.ts` line 51–54                                           | Not surfaced.                                                                                                                                                                                                              | Deferred | `server/src/protocol.rs::AgentRecord` does not carry `answerReceivedUntil`; the TUI protocol surface has no equivalent signal today. |
| Reconciliation converges rapid changes to truth within 1 s                                   | `client/src/state/store.ts` `reconcileRendered`                    | TUI reducer applies state changes synchronously in `mod.rs::run` (`table.apply(event)` before next `terminal.draw`), so the _next_ draw renders the new state.                                                             | Done     | The TUI does not tween; synchronous application is the shipped equivalent.                                                           |
| Per-agent transition cap 800 ms                                                              | `client/src/scene/transition.ts`                                   | Not ported.                                                                                                                                                                                                                | Deferred | The TUI applies state synchronously rather than tweening between states.                                                             |

## Room and chrome (wall, window, pass, board, floor, shelf, door)

| Browser treatment                                                                  | Browser source                                                                                 | TUI treatment                                                                                                                                                                                                                        | Status   | Notes                                                                                                                                                                       |
| ---------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Wall (with grouted tile)                                                           | `client/src/scene/kitchen-scene.ts` `drawRoom` lines 458–491, tiles via `grout` color          | Unified indexed backdrop (`theme::BG`) behind the pass, board, and station panels (`server/src/tui/scene/mod.rs`).                                                                                                                   | Done     | The redesign trades literal grout bands for stronger station contrast at terminal resolution.                                                                               |
| Floor + floor seams                                                                | `kitchen-scene.ts` lines 470–484, `floor`/`floorSeam` palette                                  | Station panels and the neutral outer frame define the service floor without decorative seams.                                                                                                                                        | Done     | Decorative seams were removed so half-pixel sprites and state chrome remain readable.                                                                                       |
| Window with sky/clouds                                                             | `kitchen-scene.ts` `drawWindow` lines 492–534                                                  | Not ported.                                                                                                                                                                                                                          | Deferred | Decorative at terminal scale. The TUI status header + 86 board already provide the top-of-pane context line, so dropping the window does not remove a glanceability signal. |
| 86 board (frame, "86" title, last 3 ended entries)                                 | `kitchen-scene.ts` `drawBoard` lines 535–603; rows from `snapshot().board.slice(-3)`           | `theme::BOARD` panel with `theme::BRASS` frame and `CHALK` rows; renderer reads the bounded board newest-first (`server/src/tui/scene/mod.rs`, `server/src/tui/state.rs`).                                                           | Done     | Last-3 contract identical and each row includes name, runtime, and tickets.                                                                                                 |
| Pass (steel counter, "PASS — ORDERS UP" overlay, brass/copper lamp rig, pass bell) | `kitchen-scene.ts` `drawPass` lines 636–684 + `passBellGeometry`                               | A neutral pass strip reads “all stations clear” normally and becomes a high-contrast `‼ BLOCKED {name} {MM:SS}` banner during escalation.                                                                                            | Done     | Lamp and bell silhouettes are omitted; the textual banner is immediate and color-independent.                                                                               |
| Shelf + door                                                                       | `kitchen-scene.ts` `drawShelfAndDoor` lines 604–635; `doorGeometry` shows ajar when mode=empty | Not ported.                                                                                                                                                                                                                          | Deferred | Decorative at cell scale. Empty-mode ajar signal already surfaces via the `status_lines` "Waiting for agents" overlay (`server/src/tui/view.rs`).                           |
| Status header (mode + source)                                                      | `client/src/chrome/Chrome.tsx` `ModeTreatment`; `server/src/tui/view.rs` `status_lines`        | TUI uses `view::status_lines` directly inside the scene overlay path so both panes share truthful copy. `MISE — DEMO SERVICE` / `MISE — LIVE` come from the same function (`server/src/tui/scene/mod.rs`, `server/src/tui/view.rs`). | Done     | Single source of truth — verbatim text, deterministic.                                                                                                                      |

## Stations and cooks

| Browser treatment                                                                                      | Browser source                                                                         | TUI treatment                                                                                                                                                                                                                                                                          | Status | Notes                                                                                                        |
| ------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | ------------------------------------------------------------------------------------------------------------ |
| Connected 14×21 cook silhouette (shoes/legs, apron, arms, face, two-tier toque)                        | `client/src/scene/kitchen-scene.ts` `drawCookSilhouette` lines 1290–1343               | Exact 11×14 half-pixel sprite per active state, rendered with `▀` — `server/src/tui/scene/sprites.rs`.                                                                                                                                                                                 | Done   | Shared baseline preserves toque, face, coat, apron, legs, and boots at terminal scale.                       |
| Idle pose set: smoke / recline / sleep / prep with 2-frame chop/wipe loop, smoke `Z`s, billows         | `client/src/scene/idle-poses.ts` `IDLE_POSES`, `sampleIdlePose`, `IdlePoseAssignments` | TUI idle swaps `SLEEP_A`/`SLEEP_B` every 16 ticks (closed eyes + rising Z); working swaps `PREP_A`/`PREP_B` every 4 (`server/src/tui/scene/sprites.rs`).                                                                                                                                 | Done   | Two explicit frames preserve readable motion without tweening. Browser still has the five idle poses.        |
| Working cook + pot + flame flicker (flameHighDark / flameDark alternating) + ticket with progress fill | `kitchen-scene.ts` lines 951–964 + ticket progress fill 899–905                        | Working sprite plus pinned paper ticket, pot, alternating flames, and a bounded deterministic four-slot steam pool (`scene/mod.rs`, `scene/particles.rs`).                                                                                                                             | Done   | No per-frame progress fill; the physical ticket and `FIRE` footer carry the state.                           |
| Blocked cook at pass, red ring arcs, blocked ticket, 7×13.4 (or 7×7 dense) px timer chip               | `kitchen-scene.ts` lines 965–995; `blockedPassGeometry` lines 124–155                  | Angry blocked pose, pass banner, elapsed station banner, red double-line station border, and `AT THE PASS` footer; the outer frame remains neutral.                                                                                                                                    | Done   | Four local/textual signals preserve blocked glanceability without misrepresenting the whole room as blocked. |
| Done cook + plate ellipse + lamp rays + green progress fill                                            | `kitchen-scene.ts` lines 996–1017; `donePlateGeometry` lines 172–190                   | Plated pose, green service strip, and `PLATED ✓` station footer.                                                                                                                                                                                                                       | Done   | Lamp rays are omitted; the raised plate and checkmark carry the completed reading.                           |
| Ended: cook exits, chalk row appended to 86 board                                                      | `kitchen-scene.ts` lines 686–718 disposal path; `drawBoard` row                        | Ended records are removed from the active roster and pushed onto the bounded 86 board; `cook_sprite(Ended)` returns an empty sprite (`server/src/tui/scene/sprites.rs::ENDED`). Verified by `ended_has_no_active_cook_sprite` and `ended_moves_to_board_and_truthful_status_survives`. | Done   | No active-station sprite for ended — same as browser.                                                        |
| Accent color per agent (12-token accent ramp)                                                          | `client/src/theme/tokens.ts` `accents` lines 14–27                                     | Six indexed scene accent pairs wrap deterministically; the compact table preserves the original 12 RGB accents (`server/src/tui/theme.rs`).                                                                                                                                            | Done   | Indexed pairs guarantee exact xterm-256 output in the scene while retaining the full compact-table palette.  |
| Station name (workspace) + state identity label                                                        | `kitchen-scene.ts` lines 1039–1046; `stationIdentityLabels`                            | Per-station overlay (`scene/mod.rs` lines 336–352).                                                                                                                                                                                                                                    | Done   | Verbatim copy.                                                                                               |

## Tickets and 86 board content

| Browser treatment                                                          | Browser source                                                                           | TUI treatment                                                                                                                   | Status   | Notes                                                                                                                                            |
| -------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------ |
| White ticket (idle excluded), shadow stripes for ticket lines              | `client/src/scene/geometry.ts` `stationTicketGeometry`; `kitchen-scene.ts` lines 879–911 | Working stations draw a pinned 4×6 half-pixel paper ticket with line marks; every station also has a colored ticket-count chip. | Done     | The physical ticket is limited to working state; the ticket-count chip persists across states.                                                   |
| Working ticket progress fill (green)                                       | `kitchen-scene.ts` lines 899–905                                                         | Not ported.                                                                                                                     | Deferred | `AgentRecord` carries progress, but a meaningful per-frame fill does not survive the 3×2-cell ticket budget; state copy surfaces "FIRE" instead. |
| Done ticket checkmark                                                      | `kitchen-scene.ts` lines 906–910                                                         | `✓` glyph on the per-station overlay.                                                                                           | Done     | Same semantic, smaller representation.                                                                                                           |
| Blocked ticket at the pass + timer chip + elapsed text                     | `kitchen-scene.ts` lines 965–995; `formatElapsed`                                        | Pass banner plus per-station elapsed banner and double-line border (`server/src/tui/scene/mod.rs`).                             | Done     | `MM:SS` is derived from injected `now` versus `state_entered_at`; first-frame visibility is regression-tested.                                   |
| 86 board rows: last 3 ended agents with `COOK`/`MISE TIME` columns | `client/src/scene/kitchen-scene.ts` `drawBoardRow`                                       | `board_line` fits uppercase name and mise-observed elapsed time beneath the same labeled columns.                                | Done     | Both scenes render the newest three rows; zero mise time uses `—`. Live runtime is first-seen-to-now, not a Herdr field.                         |

## Particles and motion

| Browser treatment                                                                                  | Browser source                                                                                     | TUI treatment                                                                                                                      | Status   | Notes                                                                                       |
| -------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------- | -------- | ------------------------------------------------------------------------------------------- |
| Steam particles above working stations, fixed 48-slot pool, 900 ms life                            | `client/src/scene/particles.ts` `ParticlePool`; `kitchen-scene.ts` `drawParticles` lines 1224–1235 | Four deterministic slots, eight-tick life, two-tick phase spacing, and bounded upward drift (`server/src/tui/scene/particles.rs`). | Done     | No RNG or wall clock; the compact pool is reconstructed from `(tick, origin)` on each draw. |
| Scene motion policy (reduced motion disables idle/steam/cook/escalation/travel/busser/transitions) | `kitchen-scene.ts` `sceneMotionPolicy` lines 145–156                                               | Not ported.                                                                                                                        | Deferred | TUI has no `prefers-reduced-motion` channel.                                                |
| Visual update interval 125 ms                                                                      | `kitchen-scene.ts` `visualInterval` line 1056                                                      | TUI tick = 100 ms (`SCENE_TICK_INTERVAL` in `mod.rs` line 31).                                                                     | Done     | Faster cadence, 10 Hz.                                                                      |

## Escalation

| Browser treatment                                                     | Browser source                                      | TUI treatment                                                                                                  | Status   | Notes                                                                                        |
| --------------------------------------------------------------------- | --------------------------------------------------- | -------------------------------------------------------------------------------------------------------------- | -------- | -------------------------------------------------------------------------------------------- |
| Pass bell pulse (arcs at radius 3.5/5.5/7.5 units, period 280/560 ms) | `kitchen-scene.ts` `drawEscalation` lines 1161–1223 | Static red double-line border around each blocked station plus pass/station banners; room frame stays neutral. | Done     | Local escalation preserves ownership while the elapsed text carries timing semantics.        |
| Stage 1 elapsed-time fill (≥`escalationFastMs`)                       | `kitchen-scene.ts` lines 1173–1208                  | Not ported.                                                                                                    | Deferred | No second-stage threshold; elapsed station and pass text remain continuously visible.        |
| Stage 2 edge vignette                                                 | `kitchen-scene.ts` lines 1209–1222                  | Not ported.                                                                                                    | Deferred | Vignette adds no information once blocked stations have distinct borders, pose, and banners. |

## Busser / ended sweeps

| Browser treatment                                 | Browser source                                                                                                        | TUI treatment | Status   | Notes                                                                                                                                                                                                            |
| ------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------- | ------------- | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Busser sweep animation when an ended agent clears | `kitchen-scene.ts` `BusserSweepTimeline`, `busserSweepSample`, `onStoreEvent` 1097–1117; `drawBusserSweeps` 1118–1153 | Not ported.   | Deferred | TUI's ended path is synchronous — the agent disappears from the active roster and a chalk row appears on the 86 board in the same draw frame. No intermediate "swept out of frame" state at the cell resolution. |

## Motion / accessibility / pointer / audio / chrome

| Browser treatment                                                                          | Browser source                                                    | TUI treatment                                                                                                                                                                                    | Status   | Notes                                                                                                                                                                |
| ------------------------------------------------------------------------------------------ | ----------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Reduced-motion preference                                                                  | `kitchen-scene.ts` `ReducedMotionPreference`; `sceneMotionPolicy` | No equivalent env / preference.                                                                                                                                                                  | Deferred | See motion row above.                                                                                                                                                |
| `prefers-color-scheme` light/dark                                                          | `kitchen-scene.ts` `systemDark`, `resolvedTheme`, `paletteIndex`  | TUI uses injected `ColorMode` from `terminal_capabilities` (`Truecolor` vs `Xterm256`) decided at startup.                                                                                       | Done     | Light/dark theme swap has no pty signal in scope; the scene uses six exact indexed accent pairs and the compact table retains the 12 RGB accents.                    |
| Live region `aria-label="Agent state announcements" aria-live="polite" aria-atomic="true"` | `client/src/chrome/Chrome.tsx`                                    | Not ported.                                                                                                                                                                                      | Deferred | A TUI has no aria surface. The `‼ BLOCKED MM:SS ‼` banner is the assistive glanceability equivalent.                                                                 |
| Keyboard station inspection                                                                | `kitchen-scene.ts` `pointerHandler`, `hitTest`                    | `Tab`/`BackTab` cycles stations and opens a bounded fact strip; `Esc` dismisses it before quitting.                                                                                              | Done     | Keyboard inspection exposes only feed-backed station facts.                                                                                                          |
| Pointer hit testing: select cook → detail card, open settings, board row tap               | `kitchen-scene.ts` `pointerHandler`, `hitTest`                    | Not ported.                                                                                                                                                                                      | Deferred | Pointer hit testing remains outside the keyboard-only TUI contract.                                                                                                  |
| Pointer cursor / focus rectangle                                                           | `kitchen-scene.ts` lines 1018–1037                                | Not ported.                                                                                                                                                                                      | Deferred | No pointer cursor in a pty. The blocked border + label play the "this needs attention" role.                                                                         |
| Audio: pass bell sample on blocked escalation                                              | `client/src/chrome/Audio.tsx` (audited separately)                | Not ported.                                                                                                                                                                                      | Deferred | Terminals don't have a documented audio channel. The visual escalation is the contract.                                                                              |
| Chrome / mode treatments (demo placard, live chip, disconnected banner, settings panel)    | `client/src/chrome/Chrome.tsx`, `client/src/chrome/Settings.tsx`  | Truthful copy (demo placard, source line, warning footer, `MISE — DEMO SERVICE` / `MISE — LIVE`) is shared via `view::status_lines` reused by the scene overlay (`server/src/tui/scene/mod.rs`). | Done     | Settings panel + disconnected banner do not exist in the TUI; unsupported scene rendering falls back to the compact status table.                                    |
| FPS / draw-call instrumentation                                                            | `kitchen-scene.ts` `instrumentDrawCalls`                          | No equivalent runtime instrumentation in the TUI.                                                                                                                                                | Deferred | Final acceptance instead exercised the release binary through a 1,801-second RSS soak with 31 samples; that is operational evidence, not a committed microbenchmark. |

## Terminal color / capability / fallback

| Browser treatment                                                   | Browser source                                            | TUI treatment                                                                                                                                                                                                                                                                           | Status | Notes                                                                                                                                                                                                 |
| ------------------------------------------------------------------- | --------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Truecolor path                                                      | PixiJS renderer + theme palette                           | `ColorMode::Truecolor` selected by `startup_terminal_capabilities` when `COLORTERM` contains `truecolor` or `24bit` (`mod.rs` lines 44–52).                                                                                                                                             | Done   | Same logic as `client/src/theme/theme.ts`.                                                                                                                                                            |
| xterm-256 cube fallback                                             | (browser has no equivalent — assumes canvas RGB)          | `rgb_to_xterm256` does an exhaustive nearest-color search over all 256 palette indices (`server/src/tui/canvas.rs` lines 22–38). Ties resolve to the lowest index (e.g. `rgb(4,4,4)` → index `0`). Verified by `color_modes_emit_truecolor_and_nearest_xterm_palette_with_stable_ties`. | Done   | No grey-ramp/Rec.601 tie-break — the implementation is brute-force nearest, lower-index tie.                                                                                                          |
| Degraded terminal (`TERM=dumb` or `NO_COLOR` set) → table fallback  | (browser has no equivalent)                               | `scene_supported: false` skips the scene draw and calls `view::draw` (`server/src/tui/scene/mod.rs`).                                                                                                                                                                                   | Done   | The same fallback path is exercised by `responsive_boundaries_and_compact_fallback_are_rendered`; degraded-capability behavior is also covered by TUI startup capability tests.                       |
| Min pane size gate (≥80 cols, ≥48 px height → scene; below → table) | (browser has no equivalent; resizes via `ResizeObserver`) | `LayoutDecision::Scene` vs `LayoutDecision::Fallback` (`server/src/tui/scene/layout.rs` lines 43–103). Empty 0-agent pane at sufficient size renders a truthful waiting scene shell.                                                                                                    | Done   | Verified by `exhaustive_terminal_layout_is_bounded_or_explicit_fallback` (width 40..=300 × height 10..=80 × agents 0..=20 = 389,151 decisions) and `sufficient_empty_pane_is_a_truthful_scene_shell`. |
| Resize without stale pixels                                         | `kitchen-scene.ts` `ResizeObserver` + `redraw`            | `mod.rs::run` redraws every 100 ms with the current area; canvas `clear()` fills the entire pixel buffer.                                                                                                                                                                               | Done   | Final acceptance exercised large → compact → large on one real Herdr pane and verified no stale fallback cells after the resize-back.                                                                 |

## Roster and identity surface

| Browser treatment                                                     | Browser source                                                                 | TUI treatment                                                                                                                                                                                                                                     | Status | Notes                                                                                                    |
| --------------------------------------------------------------------- | ------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | -------------------------------------------------------------------------------------------------------- |
| `sceneIdentityHash` FNV-1a (used for per-agent idle pose / pass lane) | `client/src/scene/geometry.ts` lines 192–199                                   | TUI `compute_layout(width, pixel_height, agent_count)` is deterministic on `(width, pixel_height, count)` only — no FNV hash needed. Stations are placed row-major from stable `AgentTable` order (`server/src/tui/scene/layout.rs` lines 84–94). | Done   | Same stable ordering without redundant hashing.                                                          |
| Accent index per id (token ramp modulo)                               | `client/src/scene/kitchen-scene.ts` `accentIndexForId` (referenced indirectly) | `agent.accent_index` from `AgentRecord` (`server/src/protocol.rs`); `theme::accent(agent.accent_index)` (`scene/mod.rs` line 95).                                                                                                                 | Done   |                                                                                                          |
| Demo roster (Codex, Claude, Hermes, OpenClaw, Gemini, Aider)          | `server/src/demo.rs` lines 9–40                                                | Same `demo.rs`; `--tui` falls through to demo with no socket (`HERDR_MISE_DEMO_COUNT` caps at 12).                                                                                                                                                | Done   | Shared source — both the browser's `?preset=mixed&agents=6` and the `--tui` demo use the same demo feed. |

## Audit summary

| Bucket                                                          |   Done | Deferred |  Total |
| --------------------------------------------------------------- | -----: | -------: | -----: |
| Truth treatments and state machine                              |      3 |        2 |      5 |
| Room and chrome (wall, window, pass, board, floor, shelf, door) |      5 |        2 |      7 |
| Stations and cooks                                              |      8 |        0 |      8 |
| Tickets and 86 board content                                    |      4 |        1 |      5 |
| Particles and motion                                            |      2 |        1 |      3 |
| Escalation                                                      |      1 |        2 |      3 |
| Busser / ended sweeps                                           |      0 |        1 |      1 |
| Motion / accessibility / pointer / audio / chrome               |      4 |        5 |      9 |
| Terminal color / capability / fallback                          |      5 |        0 |      5 |
| Roster and identity surface                                     |      3 |        0 |      3 |
| **Total**                                                       | **35** |   **14** | **49** |

Deferrals are deliberate and non-load-bearing for the pane's two
non-negotiables (blocked glanceability, bounded payload). No row is
undocumented.

## Cross-references

- Implementation: `server/src/tui/scene/{mod,layout,sprites,particles}.rs`,
  `server/src/tui/{mod,canvas,theme}.rs`.
- Goldens: `server/tests/goldens/scene-*.txt`.
- Capture sources: `scripts/tui-demo.tape`, `scripts/capture-tui-demo.sh`,
  `scripts/capture-tui-states.sh`.
- Captured media: `docs/assets/herdr-mise-tui-demo.gif`,
  `docs/assets/herdr-mise-tui-{live,blocked,compact}.png`,
  `scripts/tui-demo.tape.capture.json`, and `scripts/tui-states.capture.json`.
